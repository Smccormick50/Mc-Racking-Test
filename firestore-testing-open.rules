// TEMPORARY TESTING RULES ONLY. Use these only while first connecting the test site.
// Replace with firestore.rules before real use.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
